{
    "buildCommand": "npm --workspace @papergrid/core run build && npm --workspace @papergrid/web run build",
    "outputDirectory": "packages/web/dist",
    "installCommand": "npm install --include=optional",
    "framework": null,
    "rewrites": [
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}
