# Implementation Plan: AI Layout Generation v2

**Track ID:** ai-layout-v2_20260330
**Spec:** spec.md
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

Upgrade blocks and AI in vertical slices. Each phase upgrades one block type end-to-end (schema → renderer → AI prompt → references), ensuring each slice is independently shippable.

---

## Phase 1: Calendar v2 — Events Per Day

Upgrade Calendar from a highlight-only mini-month to a functional calendar with per-day events.

### Tasks

- [x] Task 1.1: Add `events` array to CalendarData schema in `packages/core/src/schemas.ts` — `events: Array<{day: number, title: string, color?: string}>`
- [x] Task 1.2: Update CalendarBlock renderer in `packages/web/src/components/planner/CalendarBlock.tsx` — show event dots/labels on days, add event input on day click
- [x] Task 1.3: Update Gemini responseSchema in `packages/convex/convex/http.ts` — add `events` to calendarData schema
- [x] Task 1.4: Update AI prompt rules in `packages/convex/convex/http.ts` — instruct AI to pre-fill calendar events from prompt context
- [x] Task 1.5: Update reference layouts that use CALENDAR to include sample events
- [x] Task 1.6: Update AILayoutResponseSchema in `packages/core/src/schemas.ts` to accept events

### Verification

- [x] Generate a layout with "March exam schedule" — Calendar shows pre-filled exam dates
- [x] Existing layouts with CALENDAR still render correctly
- [x] `tsc --noEmit` passes

---

## Phase 2: WeeklyView v2 — Tasks Per Day

Upgrade WeeklyView from single textarea per day to structured task lists.

### Tasks

- [x] Task 2.1: Add `tasks` array to WeeklyViewDay schema in `packages/core/src/schemas.ts` — `tasks?: Array<{text: string, checked: boolean}>`
- [x] Task 2.2: Update WeeklyViewBlock renderer — show task checkboxes below textarea, add task button
- [x] Task 2.3: Update Gemini responseSchema — add `tasks` to weeklyViewData.days items
- [x] Task 2.4: Update AI prompt rules — instruct AI to pre-fill day tasks from prompt context
- [x] Task 2.5: Update reference layouts that use WEEKLY_VIEW to include sample tasks

### Verification

- [x] Generate "weekly meal prep planner" — WeeklyView days show pre-filled prep tasks
- [x] Existing WeeklyView layouts still render correctly
- [x] `tsc --noEmit` passes

---

## Phase 3: Smart Pre-fill for All Blocks

Upgrade AI prompt engineering to generate contextual, useful content for every block type.

### Tasks

- [x] Task 3.1: Rewrite AI prompt "DESIGN RULES" section — add explicit instructions for contextual pre-filling of TimeBlock entries, HabitTracker habits, GoalSection goals, GRID rows, CHECKBOX items, TEXT blocks
- [x] Task 3.2: Add prompt rule: "Parse the user's prompt for specific dates, names, topics, and use them to pre-fill block content. Never leave blocks empty."
- [x] Task 3.3: Update all reference layouts to have richer, more realistic pre-filled content (not "___" placeholders)
- [x] Task 3.4: Add 3-5 new reference layouts for common requests that currently generate weak results (travel planner, project sprint, recipe collection, reading log, workout program)

### Verification

- [x] Generate 5 different prompts and verify all blocks have contextual content (no empty blocks, no "___" placeholders)
- [x] Side-by-side comparison: same prompt before/after shows meaningful improvement
- [x] `tsc --noEmit` passes

---

## Phase 4: Multi-Page Generation

Enable a single prompt to produce multiple coordinated notebook pages.

### Tasks

- [x] Task 4.1: Design multi-page response schema — extend AI response to return `pages: Array<{title, paperType, themeColor, blocks}>` with optional single-page backward compat
- [x] Task 4.2: Update Zod validation in `packages/core/src/schemas.ts` — add `AIMultiPageResponseSchema`
- [x] Task 4.3: Update Gemini responseSchema in http.ts to support pages array
- [x] Task 4.4: Update AI prompt rules — instruct when to generate multi-page (weekly planner = 7+1 pages, project tracker = overview + detail pages)
- [x] Task 4.5: Update `generateLayout` in `packages/web/src/services/geminiService.ts` to handle multi-page responses
- [x] Task 4.6: Update Dashboard to receive and create multiple pages from one generation

### Verification

- [x] Generate "weekly planner" — produces overview page + 7 daily pages
- [x] Generate "study planner" — produces single page (backward compat)
- [x] All pages have contextual content
- [x] `tsc --noEmit` passes

---

## Final Verification

- [x] All acceptance criteria met
- [x] TypeScript compiles cleanly
- [x] All 4 phase verifications pass
- [x] Existing layouts not broken (regression check)
- [x] Reference layouts updated and consistent

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
